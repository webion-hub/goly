import 'package:flutter/material.dart';
import 'package:goly/models/goal.dart';
class GoalCard extends StatelessWidget {
  final GoalModel g;
  const GoalCard({super.key, required this.g});

  @override
  Widget build(BuildContext context) {
    return Card();
  }
}